<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BadmintonBoard.MainPage"
             >

    <Grid>
        <!--<Image Source="shuttlecock.png" Opacity=".05" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>-->

        <Grid VerticalOptions="Center" HorizontalOptions="Center" BackgroundColor="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Text="Create Board" Command="{Binding CreateBoardCommand}" WidthRequest="200"/>
            <Button Grid.Row="1" Text="Manage Players" Command="{Binding ManagePlayersCommand}" WidthRequest="200"/>
            <Button Grid.Row="2" Text="Manage Venues" Command="{Binding ManageVenuesCommand}" WidthRequest="200"/>
        </Grid>
    </Grid>
</ContentPage>